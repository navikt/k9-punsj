spring:
  profiles:
    active: test
  flyway:
    enabled: true
  datasource:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 3
    driver-class-name: org.postgresql.Driver

server:
  port: 8080
