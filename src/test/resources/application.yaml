spring:
  main:
    web-application-type: reactive
  profiles:
    active: test
  flyway:
    enabled: true

server:
  port: 8085

logging:
  level:
    web: TRACE
    org:
      springframework:
        web: TRACE
